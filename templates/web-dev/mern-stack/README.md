# MERN stack (Bootnode)

## API server

```bash
cd server
npm install
npm start
```

Runs on [http://localhost:5000](http://localhost:5000) · `GET /api/health`

## React client

```bash
cd client
npm install
npm run dev
```

Runs on [http://localhost:5173](http://localhost:5173).
